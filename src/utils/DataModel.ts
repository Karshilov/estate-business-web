export interface UserInfoModel {
    username: string;
    email:string;
    avatar: string;
}

export interface SearchItemModel {
    id: string;
    title: string;
    neighborhood: string;
    cover: string;
    area: string;
    floor: string;
    total_floor: string;
}